return {
  -- 'mikeyloboto/cmp-ai-evilchat',
  -- dependencies = 'nvim-lua/plenary.nvim',
  -- config = function()
    -- local cmp_ai = require 'cmp_ai_evilchat.config'
    -- cmp_ai:setup {
      -- max_lines = 1000,
      -- provider = 'EvilChat',
      -- notify = true,
      -- notify_callback = function(msg)
        -- vim.notify(msg)
      -- end,
      -- run_on_every_keystroke = true,
      -- ignored_file_types = {
        -- -- lua = true,
      -- },
    -- }
  -- end,
}
